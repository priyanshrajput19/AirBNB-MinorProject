"""
This package holds all FastAPI route definitions for version 1 of the API.
Each module inside `routes/` wires URL paths to controller functions.
Example: `listings.py` will define a router with endpoints like GET /api/v1/listings.
"""
