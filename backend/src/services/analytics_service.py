"""
Analytics-specific helper functions.

Ideas for future content:
    - `calculate_average_price(clean_records)` → returns overall and per-city averages.
    - `build_trend_series(clean_records, metric="price")` → creates monthly data points.
    - `compare_countries(country_a, country_b)` → side-by-side stats for dashboards.

Controllers can call these helpers to keep their own code small and readable.
"""
